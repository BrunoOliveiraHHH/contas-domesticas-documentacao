# Tarefa — Projeto Quasar CLI + estrutura · Frontend
> Sprint 1 (06/07–20/07/2026) · Bloco: Fundação Quasar · Passo 1/4 · Depende: —

## O que fazer
Ter o projeto base Quasar CLI (Vite) rodando, com a estrutura de pastas e a variável da API.

## Passo a passo
1. Projeto Quasar (Vite) com Pinia, Vue Router e Axios (o scaffold base já vem no repositório).
2. Definir `API_URL` via `.env` / `quasar.config.js` (`build.env`).
3. `npm install` e `quasar dev` sobem a aplicação.

## Onde mexer
- `quasar.config.js`, `.env`, `src/`

## Critério de pronto (DoD)
- [ ] `quasar dev` sobe a app; `quasar build` gera o dist

## Como testar
`quasar dev` no navegador.
